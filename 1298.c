#include <stdio.h>
int max(int a, int b) {
    if(a>b) return a;
    else return b;
}
int main() {
    int a, b, c, i, j;
    scanf("%d %d %d", &a, &b, &c);
    for(i=1;i<=c/max(a,b);i++) {
        for(j=1;i<=c/max(a,b))
    }
}